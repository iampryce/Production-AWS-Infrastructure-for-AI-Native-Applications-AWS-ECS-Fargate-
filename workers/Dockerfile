# Placeholder for Phase 5 (decoupled deploy pipeline) - same reasoning as
# backend/Dockerfile. Stays alive, does nothing. Replaced by the real
# Celery worker in Phase 11.
FROM python:3.12-slim

CMD ["sleep", "infinity"]
