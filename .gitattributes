# EC2 user_data / boot scripts must stay LF - CI applies from a Linux
# checkout, and a CRLF-vs-LF byte difference changes the templatefile()
# hash Terraform uses to decide whether to replace the instance, causing
# spurious "must be replaced" plans on Windows checkouts.
*.tpl text eol=lf
*.sh text eol=lf
