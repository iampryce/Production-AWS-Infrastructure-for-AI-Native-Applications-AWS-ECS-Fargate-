# Copy to .env.local for local dev. Defaults to the local backend
# (docker-compose); point it at the real deployed API to prove the
# pipeline works end to end against live infra instead.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
